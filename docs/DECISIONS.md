# Decisions — Personal Site

> Lab 02 · สังเคราะห์จาก [`DEBATE.md`](DEBATE.md) (Agent Teams 3 บทบาท · 5 รอบ) · 2026-09-25 · ไฟล์นี้ = **Approved**
> หมายเหตุ: STATUS เดิมอ้างถึง `DECISIONS.md` D1–D9 จากเซสชันก่อน แต่**ไม่พบไฟล์นั้น**ทั้งบนดิสก์และใน git history · ไฟล์นี้เริ่มนับ D-id ใหม่ และถือเป็นฉบับเดียวที่ใช้ได้ · headline / Tagline / Tone ใน PROFILE ที่แก้ค้างอยู่ใช้เป็น input ของการถกรอบนี้

## สรุปการโต้วาที

ทั้งสามบทบาทเห็นตรงกันว่าจุดต่างของแบรนด์คือ "full-stack ที่คุมทีม AI agent ได้อย่างมีระบบ" แต่ Devil's Advocate ชี้ว่าคำเคลมอย่าง "ครบทุกชั้น", "5 ปี" และ "ตรวจสอบได้ใน repo" จะเป็น overclaim ถ้าไม่บอกว่าใครทำอะไรหรือยังไม่มีหลักฐาน ข้อยุติคือคงคำหลักไว้โดยต้องมีประโยคบอกบทบาทกำกับทุกครั้ง และให้คำเคลมกับลิงก์ที่ต้องอาศัยหลักฐานเปิดได้ก็ต่อเมื่อผ่าน gate แล้ว ประเด็นที่ถกกันหนักที่สุดคือหน้า Contact ผลคือหน้านี้เป็น Must แต่ตัวฟอร์มยังไม่เป็น Must (ship ได้แบบซ่อนฟอร์ม) และวันที่ ship ต้องมีช่องทางติดต่อที่ใช้งานได้จริงอย่างน้อยหนึ่งช่อง UX ดึงการตัดสินใจกลับมาที่มือถือและ contrast ส่วน Brand ถอยเรื่องความยาว headline และยอมตัด dropdown

## การตัดสินใจ (ตาราง)

| ID | หัวข้อ | ตัดสินใจ | เหตุผลสั้น | ใครเสนอ (Brand/UX/Devil) |
|----|--------|----------|------------|---------------------------|
| D1 | Headline | `## Headline` = "Full-stack developer ที่วางระบบให้ทีม AI agent ส่งงานครบทุกชั้น" · UI แสดงใน `<h1>` เดียว 2 ระดับ: "Full-stack developer" ตัวใหญ่ + ส่วนที่เหลือตัวรอง · **บรรทัดรองห้ามซ่อนทุกขนาดจอ** · "ครบทุกชั้น" ห้ามใช้เดี่ยวโดยไม่มีคำขยายบทบาท | รวมเป็นเสียงเดียว กวาดตาได้บนจอ 360px และไม่ overclaim | Brand (B1) + UX (X1) + Devil (C1) |
| D2 | Tagline | `## Tagline` = "ผมเขียน spec และ review ส่วน agent แยกกันทำ frontend / backend — ตั้งแต่ UI ถึง deploy" · "ถึง deploy" ต้องเป็นจริงในวัน ship ไม่อย่างนั้นตัดออก · วลี "ตรวจสอบได้ใน repo" เติมได้หลังผ่าน D8 เท่านั้น | tagline ทำหน้าที่เป็นหลักฐานว่าใครทำอะไร (R2) | Brand + Devil |
| D3 | Bio | ไม่เกิน 3 ย่อหน้า: คุณค่า → ประสบการณ์ (**ไม่มีตัวเลขปี**) → CTA "ดูโค้ดเว็บนี้ แล้วทักมาทางช่องทางติดต่อได้เลยครับ" · ไม่เปิดด้วยคำทักทาย · ไม่เล่ารายละเอียด workflow · About ต้องมีประโยค "frontend / backend เขียนโดย agent ภายใต้ spec และ review ของผม" | ปิด R1 / R8 และลดระดับ R2 | Brand (B3) + Devil |
| D4 | Audience / CTA | Home พูดกับ recruiter / hiring manager เท่านั้น · ปุ่มส้มปุ่มเดียว "ติดต่อผม" เห็นได้โดยไม่ต้องเลื่อนจอ · กลุ่มรองพิมพ์บอกเองในฟอร์ม | ข้อความโฟกัส และตรงกับกฎว่าสีส้มใช้เฉพาะปุ่มติดต่อ | Brand + UX (U3, U6) |
| D5 | IA 4 หน้า | Home (D1, D2, D4) · About (D3 → Interests แบบย่อ + ลิงก์ "ดูรายละเอียด" → CTA) · **Interests คงเป็นหน้าแยก** แสดง "หัวข้อ — ประโยคเดียว" (bullet บรรทัดเดียวเพื่อให้ parser อ่านได้) · Contact ตาม D6 | route `interests.astro` และ `GET /api/interests` มีอยู่แล้ว (API เป็น ownership ของ backend) · ผลโหวต 2:1 | UX (U1, U5) + Devil (X3) · Brand ค้าน |
| D6 | ฟอร์ม Contact | 3 ช่อง (ชื่อ / อีเมล / ข้อความ) · **ไม่มี dropdown** · placeholder "เล่าสั้น ๆ ว่าอยากคุยเรื่องอะไรครับ" · ตัวนับ 0/1000 · กัน spam ด้วย honeypot ที่มองไม่เห็น + rate limit ฝั่ง server (ห้ามใช้ CAPTCHA) · success "ได้รับข้อความแล้วครับ" (ไม่สัญญาว่าจะตอบกลับจนกว่าจะผ่าน D7) · error ทุกกรณีบอกทางเลือกถัดไป ไม่มีรหัส / stack / SQL | ฟอร์มสั้น ลด bot และไม่มีข้อความที่หลอกผู้ใช้ | UX (U4, U7, U9) + Devil (R4) |
| D7 | Contact 2 สถานะ | **A (ค่าเริ่มต้น / API ตอบ 501):** ซ่อนฟอร์ม แสดงช่องทางสำรองพร้อมข้อความ "ช่องทางนี้กำลังเตรียมพร้อมครับ" · **B:** เปิดฟอร์มเมื่อครบทุกข้อ ได้แก่ เจ้าของคือ nopame · เปิดอ่านอย่างน้อยสัปดาห์ละครั้งด้วยวิธีที่ไม่เปิดสาธารณะ · ลบข้อมูลหลัง 90 วัน · มีอีเมลตอบกลับจริง (ไม่ใช่ demo) · มีประโยค retention "ข้อความจะเก็บไว้เพื่อติดต่อกลับเท่านั้น และลบภายใน 90 วัน" | ปิดความเสี่ยง R5 "ฟอร์มหลุมดำ" ได้โดยไม่ขวาง ship | UX + Devil |
| D8 | Audit ก่อนลิงก์ GitHub หรือคำว่า "ตรวจสอบได้" | ต้องผ่านครบ 5 ข้อ: (1) `git log --all` ไม่มี `.env` / PAT / webhook (2) author name / email ทุก commit เป็นนามแฝงหรือ noreply (3) โปรไฟล์ GitHub ไม่มีชื่อจริง / บริษัท / รูปหน้า (4) CI บน main เขียว (5) README บอกว่าเป็นผลงาน · ถ้าไม่ผ่าน ให้ถอดทั้งลิงก์และคำเคลม · ลิงก์ชี้ README ของเว็บ ไม่ใช่ `labs/` · ใน UI เป็นตำแหน่งที่เปิดปิดได้ **ห้ามฝัง URL จริงก่อน audit** | ความเสี่ยง R3: ลิงก์โยงถึงตัวตนจริง หรือพาไป 404 / private | Devil (C2) + UX + Brand (B4) |
| D9 | Ship gate ช่องติดต่อ | วันที่ ship ต้องมีช่องติดต่อที่ใช้งานได้จริงอย่างน้อย 1 ช่อง (ฟอร์มสถานะ B หรือโปรไฟล์ GitHub ที่ผ่าน D8) · ถ้าไม่มีเลย **ห้าม ship** | เว็บ branding ที่ recruiter ติดต่อไม่ได้ถือว่าล้มเหลว · มติ 3:0 | Devil · Brand ถอนจุดยืน · UX เห็นด้วย |
| D10 | ช่องทางที่ว่าง | ไม่ render ช่องทางที่ค่าเป็น "—" หรือว่าง (เช่น linkedin) | "—" ทำให้เว็บดูเหมือนพัง | UX (U2) + Brand (B4) |
| D11 | ธีม / contrast | ชื่อสี (ฟ้า / มิ้นต์ / ส้ม) ไม่ขวาง ship · contrast ของตัวอักษรเข้มบนพื้นสว่างและปุ่มติดต่อที่เด่นชัดเป็น **Must** | ความอ่านง่ายเป็นเรื่อง UX ส่วนความสดใสเป็นแค่สไตล์ | UX + Devil |
| D12 | Must v1 (แทน Must ใน Brainstorm) | (1) Home ตาม D1 / D2 / D4 อ่านง่ายบนมือถือและ contrast ผ่าน (2) About ตาม D3 (3) หน้า Contact ตาม D6 / D7 + ผ่าน D9 (4) Deploy ที่ public URL ตอบ HTTP 200 จริง · **Guestbook ย้ายไป Later** · Nice ห้ามเริ่มก่อน Must ครบ | ลด scope creep (R7) และความเสี่ยง R6 | ทั้ง 3 บทบาท |
| D13 | คำหลักใน Tone | "ครบทุกชั้น · ตรวจสอบได้ · มีระบบ" ใช้ได้เฉพาะเมื่อมีหลักฐานหรือคำขยายวางอยู่ใกล้ ๆ · "ตรวจสอบได้" รอผ่าน D8 | ไม่ให้คำหลักกลายเป็น buzzword (B2) | Brand + Devil |

## การตัดสินใจเพิ่มระหว่าง implement (2026-09-25 · facilitator/frontend)

> เปิด D-id ใหม่แทนการแก้ D1–D13 เงียบ ๆ · D14 **แทนที่** ส่วน "Guestbook → Later" ใน D12

| ID | หัวข้อ | ตัดสินใจ | เหตุผลสั้น | ใครเสนอ |
|----|--------|----------|------------|---------|
| D14 | Guestbook v1 (แทนที่ D12 บางส่วน) | **คงหน้า Guestbook ใน v1** ในรูปแบบปลอดภัย: render ด้วย `textContent` เท่านั้น (ห้าม `innerHTML`) · ชื่อ ≤ 80 · ข้อความ ≤ 500 · มี honeypot + rate limit ฝั่ง server · ไม่เก็บอีเมล · แสดงล่าสุด 50 รายการ | nav, API contract และ `test:labs` ต้องมี guestbook · มาตรการของ Devil (R6) ครอบความเสี่ยง XSS / spam | facilitator (อิง R6) |
| D15 | สถานะเริ่มต้นของฟอร์ม Contact | ฟอร์มแสดงเป็นค่าเริ่มต้น · ถ้า API ตอบ 501 หรือเชื่อมต่อไม่ได้ ให้สลับเป็นสถานะ A (D7) ทันทีตอนกดส่ง · retention 90 วันต้องบังคับในโค้ด backend · เงื่อนไขฝั่งคน (มีคนอ่านทุกสัปดาห์ / มีอีเมลตอบกลับจริง) ย้ายไปเป็น ship gate (L8) | ให้ทดสอบฟอร์มได้จริงหลัง backend เสร็จ โดยไม่แสดงข้อความหลอกผู้ใช้ | facilitator (อิง D7) |
| D16 | CTA ใน Bio | ใช้ "ทักมาทางหน้าติดต่อได้เลยครับ" แทน "ดูโค้ดเว็บนี้…" จนกว่าจะผ่าน D8 | ลิงก์ repo ยังซ่อนอยู่ตาม D8 | facilitator (อิง D3 + D8) |
| D17 | สวิตช์ลิงก์ GitHub | ตั้ง env `SHOW_GITHUB_LINK=true` ตอน runtime → แสดงลิงก์โปรไฟล์ GitHub จาก `## Contact` (ต้องเป็น `https://github.com/<user>` เท่านั้น) · ค่าเริ่มต้นคือซ่อน | ช่องเปิดปิดตาม D8 ไม่ได้ฝัง URL ลงในโค้ด | frontend |

## API contract (UI ↔ backend · สำหรับ Lab 05)

| Endpoint | Request (JSON) | สำเร็จ | Error |
|---|---|---|---|
| `POST /api/contact` | `{ name ≤80, email ≤120 (รูปแบบอีเมล), message ≤1000, website? }` | `201 { ok: true }` | `400 { error }` validation · `429 { error }` rate limit · `501` ยังไม่ implement · `500 { error }` ข้อความกลาง |
| `GET /api/guestbook` | — | `200 { entries: [{ id, name, message, created_at }] }` ใหม่สุดก่อน ≤ 50 รายการ | `501` · `500 { error }` ข้อความกลาง |
| `POST /api/guestbook` | `{ name ≤80, message ≤500, website? }` | `201 { entry }` | `400` / `429` / `501` / `500` ตามแบบด้านบน |

- `error` เป็นข้อความภาษาไทยสั้น ๆ ที่ปลอดภัยต่อผู้ใช้ **ห้าม**มี stack trace, SQL หรือ `err.message` ดิบ (UI แสดง `error` เฉพาะกรณี 400/429)
- `website` คือ honeypot: ถ้ามีค่า ให้ตอบเหมือนสำเร็จ (`201`) แต่ไม่บันทึก
- ตัดช่องว่างหัวท้าย (trim) ก่อน validate · ห้ามเป็นค่าว่าง
- Rate limit ต่อ IP (เช่น 5 ครั้งต่อ 10 นาทีต่อ endpoint) ต้องมี test (D6)

## สิ่งที่เลื่อนออก (Out of scope v1)

- ~~Guestbook~~ → กลับมาอยู่ใน v1 ตาม D14 (มีมาตรการป้องกันครบ)
- dropdown "เรื่องที่ติดต่อ" ในฟอร์ม
- ตัวเลข "5 ปี" และตัวเลขใดก็ตามที่ตรวจสอบไม่ได้
- ลิงก์ LinkedIn และช่องทางที่ยังไม่มีค่า
- Nice ทั้งหมด (แถบ 5 ชั้น, badge CI, "เว็บนี้สร้างยังไง", Projects, โหมดมืด, "สำหรับมือใหม่") จนกว่า Must ใน D12 จะครบ
- Later เดิม: Blog, หน้า Services, สองภาษา

## เกณฑ์พร้อม Frontend (Lab 04)

- `docs/PROFILE.md` มี Headline / Tagline ตาม D1 / D2 แล้ว (แก้ในรอบนี้) และ parser อ่าน `## Tagline` ได้ (ยังไม่ได้ทำ: L6)
- Bio เขียนใหม่ตาม D3 ไม่เกิน 3 ย่อหน้า ไม่มีตัวเลขปี และมีประโยคบทบาท · Interests เขียนเป็น "หัวข้อ — ประโยคเดียว" ตาม D5
- UI รับมือ 501 จาก `/api/contact` ตั้งแต่แรก: สถานะ A ตาม D7 ซ่อนฟอร์มและแสดงช่องทางสำรอง
- ลิงก์ GitHub และคำว่า "ตรวจสอบได้" เป็นตำแหน่งที่เปิดปิดได้ ห้ามฝัง URL จริงจนกว่าจะผ่าน D8
- ตรวจบนจอ 360px: บรรทัดรองของ headline ไม่ถูกซ่อน และปุ่ม "ติดต่อผม" เห็นได้โดยไม่ต้องเลื่อนจอ · `npm test` (guard test) เขียว

## บันทึกการแก้ PROFILE

- `## Headline` → ข้อความตาม D1 (เดิมคือ "Full-stack developer ที่ส่งงานได้ครบทุกชั้น — ตั้งแต่ UI จนถึง deploy")
- `## Tagline` → ข้อความตาม D2 (เดิมคือ "ใช้ AI agent หลายตัวทำงานเป็นทีมอย่างมีระบบ และทุกการตัดสินใจตรวจย้อนได้ใน repo" ซึ่งอ้างว่า "ตรวจย้อนได้" ก่อนผ่าน D8)
- ไม่ได้แก้ Bio / Interests ในรอบนี้ ให้ทำตามเกณฑ์ Lab 04 ด้านบน
